{
  "name": "strata-enquiry-workflow-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Local web UI and staff dashboard for the n8n strata enquiry classifier workflow.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
